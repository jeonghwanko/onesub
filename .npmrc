# The monorepo validates host-owned native peers in the isolated SDK peer matrix.
# Consumers do not receive this repository setting, so the published SDK still
# declares React Native as a required peer without materializing it in repo CI.
legacy-peer-deps=true
